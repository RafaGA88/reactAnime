import styled from 'styled-components';

export const Nav = styled.nav`
  background-color: rgba(255, 211, 122, 0.9);
  padding: 16px;
  font-family: American Typewriter, serif;

  a {
    color: black;
    margin: 0 25px 0 0;
  }
`;

export const DivLoginLogout = styled.div`
  float: right;
  display: inline;
  margin-top: 10px;
`;

export const Title = styled.h1`
  font-size: 27px;
  display: inline;
`;
